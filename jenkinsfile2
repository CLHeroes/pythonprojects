pipeline {
    agent any
    stages {
        stage("Copy date to date.txt file") {
            steps {
                sh """ 
                    date > date.txt
                """
            }
        stage("change date.txt to today.txt") {
            steps {
                sh """
                    mv date.txt today.txt
                """
            }
        }
        }
    }
}